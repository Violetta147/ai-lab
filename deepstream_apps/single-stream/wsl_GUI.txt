sudo apt update
sudo apt install xfce4 xfce4-goodies -y
sudo apt install xrdp -y
sudo sed -i 's/^port=3389/port=3390/' /etc/xrdp/xrdp.ini
echo "startxfce4" > ~/.xsession
cat /proc/1/comm
nếu là init thay vì systemd thì:
sudo tee -a /etc/wsl.conf << 'EOF'
[boot]
systemd=true
EOF
wsl --shutdown
sudo systemctl enable xrdp
sudo systemctl start xrdp
notepad $env:USERPROFILE\.wslconfig
[general]
instanceIdleTimeout=-1

[wsl2]
vmIdleTimeout=-1
guiApplications=false
Nhấn Win + R trên Windows, gõ mstsc và nhấn Enter để mở Remote Desktop Connection.
Tại ô Computer, nhập: localhost:3390.
Nhấn Connect. Khi cửa sổ đăng nhập hiện lên, nhập Username và Password của Ubuntu 20.04. Bạn sẽ thấy giao diện Desktop XFCE4 hiện ra.
