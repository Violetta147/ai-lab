services:
  # 1. Celery Worker: Thực thi các task
  worker:
    build:
      context: ..
      dockerfile: pipeline/Dockerfile
    command: celery -A pipeline.celery_app worker --loglevel=info --concurrency=1
    restart: always
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
    environment:
      - CELERY_BROKER_URL=redis://redis:6379/0
      - CELERY_RESULT_BACKEND=redis://redis:6379/0
      - MINIO_URL=minio:9000
      - CVAT_URL=http://host.docker.internal:8080
      - DB_HOST=db
      - DB_PORT=5432
      - MQTT_BROKER=mqtt
      - MQTT_PORT=1883
      # Telegram (cấu hình qua env, không hardcode)
      - TELEGRAM_TOKEN=${TELEGRAM_TOKEN:-}
      - TELEGRAM_CHAT_ID=${TELEGRAM_CHAT_ID:-}
      # CVAT
      - CVAT_USER=${CVAT_USER:-django}
      - CVAT_PASS=${CVAT_PASS:-changeme}
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - cvat_temp_data:/app/cvat_temp
      - ./model:/app/pipeline/model
      - ..:/app
    networks:
      - mlops_traffic_net

  # 2. Celery Beat: Lập lịch cho task
  beat:
    build:
      context: ..
      dockerfile: pipeline/Dockerfile
    command: celery -A pipeline.celery_app beat --loglevel=info
    restart: always
    depends_on:
      - worker
    environment:
      - CELERY_BROKER_URL=redis://redis:6379/0
      - CELERY_RESULT_BACKEND=redis://redis:6379/0
    extra_hosts:
      - "host.docker.internal:host-gateway"
    networks:
      - mlops_traffic_net

  # 3. MQTT Listener: Nhận dữ liệu từ Jetson
  mqtt_listener:
    build:
      context: ..
      dockerfile: pipeline/Dockerfile
    command: python3 -m pipeline.services.mqtt_service
    restart: always
    environment:
      - MQTT_BROKER=mqtt
      - MQTT_PORT=1883
      - DB_HOST=db
      - DB_PORT=5432
    depends_on:
      - beat
    volumes:
      - ..:/app
    networks:
      - mlops_traffic_net

volumes:
  cvat_temp_data:

networks:
  mlops_traffic_net:
    external: true
