[RTSP-FIRST EDGE PIPELINE ACCEPTANCE CHECKLIST]

1) Laptop RTSP source is running
   ffmpeg -re -stream_loop -1 -i "D:\path\dataset.mp4" -c copy -f rtsp rtsp://localhost:8554/mystream

2) Jetson DeepStream config is RTSP input (type=4)
   rg "^\[source0\]|^type=4|^uri=rtsp://" "$HOME/deepstream_yolo/deepstream_app_yolov8_rtsp.txt"

3) Jetson output is RTSP sink only (no MP4 sink)
   rg "^\[sink0\]|^type=4|output-file|type=3" "$HOME/deepstream_yolo/deepstream_app_yolov8_rtsp.txt"
   Expectation:
   - has type=4 in sink
   - no output-file=
   - no type=3 sink

4) Jetson pipeline run is stable
   # Copy script to Jetson once, then run:
   # scp tmp/jetson_rtsp_scale_test.sh jetson@<JETSON_IP>:~/deepstream_yolo/
   bash ~/deepstream_yolo/jetson_rtsp_scale_test.sh
   Pass criteria:
   - "App run successful"
   - no recurring ERROR lines
   - sustained PERF output

5) Live monitor from laptop works
   VLC open:
   rtsp://<JETSON_IP>:8555/ds-test

6) Laptop FFmpeg recording works
   ffmpeg -rtsp_transport tcp -i rtsp://<JETSON_IP>:8555/ds-test -c copy output_jetson_processed.mp4
   Pass criteria:
   - output file is playable
   - boxes and IDs visible

7) Flicker quick-tune without retraining
   In setup_deepstream_jetson.sh:
   - PRE_CLUSTER_THRESHOLD (start 0.10, tune 0.10-0.25)
   - NMS_IOU_THRESHOLD (start 0.50, tune 0.45-0.60)

8) Final accept/reject gate
   ACCEPT if all:
   - input RTSP is stable
   - output RTSP is stable
   - laptop recording file is valid
   - flicker reduced to acceptable demo level
